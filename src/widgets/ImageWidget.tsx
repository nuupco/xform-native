/**
 * ImageWidget — stub for PR-2 implementation (REQ-M03..M06).
 *
 * PR-1: exists as a routing target only. Real implementation in PR-2.
 */

import { View, Text } from 'react-native';

export function ImageWidget() {
  return (
    <View testID="image-widget">
      <Text>ImageWidget</Text>
    </View>
  );
}

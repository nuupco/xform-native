/**
 * AudioWidget — stub for PR-4 implementation (REQ-M11..M14).
 *
 * PR-1: exists as a routing target only. Real implementation in PR-4.
 */

import { View, Text } from 'react-native';

export function AudioWidget() {
  return (
    <View testID="audio-widget">
      <Text>AudioWidget</Text>
    </View>
  );
}

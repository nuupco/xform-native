/**
 * SignatureWidget — stub for PR-3 implementation (REQ-M07..M10).
 *
 * PR-1: exists as a routing target only. Real implementation in PR-3.
 */

import { View, Text } from 'react-native';

export function SignatureWidget() {
  return (
    <View testID="signature-widget">
      <Text>SignatureWidget</Text>
    </View>
  );
}

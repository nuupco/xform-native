/**
 * FileWidget — stub for PR-4 implementation (REQ-M15..M16).
 *
 * PR-1: exists as a routing target only. Real implementation in PR-4.
 */

import { View, Text } from 'react-native';

export function FileWidget() {
  return (
    <View testID="file-widget">
      <Text>FileWidget</Text>
    </View>
  );
}
